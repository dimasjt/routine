import React from "react"
import { Platform } from "react-native"
import { Ionicons, MaterialCommunityIcons } from "@expo/vector-icons"

// const Icon = (props) => (
//   Platform.OS === "ios" ? <Ionicons {...props} /> : <MaterialCommunityIcons {...props} />
// )

export default Ionicons